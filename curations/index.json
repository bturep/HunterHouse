{
  "_note": "Index of available curators. Each entry's <slug>.json holds that curator's selection + bio + intro. The slug is internal — only the displayed `curator` string is user-visible in the chip.",
  "curations": [
    { "slug": "for-theodora", "curator": "For Theodora" }
  ]
}
